window.onload = function() {
    window.dima = baron({
        root: '#news-view',
        scroller: '.scroller',
        bar: '.scroller__bar',
        barOnCls: 'baron'
    });

    window.dima = baron({
        root: '#experts-view',
        scroller: '.scroller',
        bar: '.scroller__bar',
        barOnCls: 'baron'
    });
};